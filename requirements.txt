# Core
numpy>=1.24
tqdm>=4.65

# Web viewer
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1

# YouTube download (also needs ffmpeg available in PATH)
yt-dlp>=2024.04.09

# Audio I/O
librosa>=0.10
soundfile>=0.12

# Model inference (Qwen2.5-Omni). Versions must support the *Omni model classes.
torch>=2.1
transformers>=4.52
accelerate>=0.30
